# Production-Grade NLP/ML Libraries for Resume Matching
# Stage 1: Vector Similarity & Embeddings
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4  # Use faiss-gpu for GPU acceleration
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0

# NLP Processing
spacy>=3.7.0
# Download model: python -m spacy download en_core_web_sm

# Text Processing
nltk>=3.8.1
# Download NLTK data: python -m nltk.downloader stopwords punkt

# File Parsing Libraries (Python-based)
PyPDF2>=3.0.0  # PDF parsing
pdfplumber>=0.10.0  # Better PDF text extraction
python-docx>=1.1.0  # Word document parsing
openpyxl>=3.1.0  # Excel file parsing
pandas>=2.0.0  # Excel data processing
xlrd>=2.0.1  # Legacy Excel support (.xls)

# Stage 2: Free LLM Integration
transformers>=4.35.0
torch>=2.1.0
accelerate>=0.24.0
bitsandbytes>=0.41.0  # For quantization (optional, reduces memory)

# Utilities
python-dateutil>=2.8.2
pytz>=2023.3

# DocMind AI Integration
requests>=2.31.0  # For DocMind API calls

# Spire Document Processing Libraries
spire-doc>=11.8.0  # Word document processing (.doc, .docx)
spire-xls>=13.9.0  # Excel file processing (.xls, .xlsx)
# Note: Spire PDF is included in spire-doc package

# Advanced Document Processing
unstructured>=0.10.0  # Advanced document parsing and structure extraction
layoutparser>=0.3.4  # Document layout analysis (optional, for advanced use)
# Note: layoutparser requires additional dependencies (detectron2, etc.)

